- name: play level variables
  hosts: local
  connection: local
  vars:
    COURSE: DevOps with AWS #course=ansible shell we refer $VAR or ${VAR}
    DURATION: 10HR  #in Ansible we wirte as {{ COURSE }}
    TRAINER: JATHIN
  tasks:
  - name: print course informatiom
    vars:
      COURSE: Ansible #task level variables @inheritance
    ansible.builtin.debug:
    # vars reference should be surrounded by {{}}
      msg: "Hello {{ COURSE }}, Course Duration: {{ DURATION }}, Trainer is {{ TRAINER }}"
  
  - name: print course informatiom
    ansible.builtin.debug:
    # vars reference should be surrounded by {{}}
      msg: "Hello {{ COURSE }}, Course Duration: {{ DURATION }}, Trainer is {{ TRAINER }}"

