- name: understand vars preference
  hosts: local
  connection: local 
  vars:
    GREETING: "Hello from PLAY"
  vars_files:
  - course.yaml
  vars_prompt:
  - name: GREETING
    prompt: Please enter the GREETING
    private: false
  tasks:
  - name: print the GREETING
    vars:
      GREETING: "Hello from Task"
    ansible.builtin.debug:
      msg: "{{ GREETING }}"

      #1. Command line oe arguments
      #2. Task 
      #3. File
      #4. prompt
      #5. play